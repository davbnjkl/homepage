# Homepage

这是一个整理后的个人主页静态站点。

## 内容

- `index.html`: 主页入口。
- `build_sy/`: 合成小游戏页面及运行资源。
- `tool/`: 字符倒序工具页面。
- `cipher_game/`: 文字冒险页面。
- `images/`: 主页和子页面使用到的图片资源。

## 使用

直接打开 `index.html` 即可访问主页，也可以部署到任意静态网页托管服务。

注意：`build_sy` 是游戏构建目录，里面的 `res/`、`src/` 和引擎文件是运行时依赖，不建议删除。
